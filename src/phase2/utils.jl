"""
    sort_edges(edges)

Trie un dictionnaire d'arétes dans l'ordre croissant ou décroissant de leurs poids. Utilise un tri par fusion.

Arguments
    edges::Dict{String,Edge}: le dictionnaire d'arêtes à trier
"""
function sort_edges(edges::Dict{String,Edge})
    function merge_dicts(keys::Vector, values::Vector)
        # TODO
        return nothing
    end


    function merge_sort_dict(keys::Vector, values::Vector)
        # TODO
       return nothing
    end
end